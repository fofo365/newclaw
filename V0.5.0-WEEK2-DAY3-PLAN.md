# NewClaw v0.5.0 Week 2 Day 3 开发计划

**日期**: 2026-03-11 (周二)
**开发人员**: AI Agent (GLM-5)
**任务**: Week 2 Day 3 - 集成测试 + 工具发现机制

---

## 📋 任务清单

### P0 任务

#### 1. 工具发现机制 (2 小时)
- [ ] 实现 `tools/list` 接口
- [ ] 实现 `resources/list` 接口
- [ ] 实现 `prompts/list` 接口
- [ ] 添加发现接口测试

#### 2. 集成测试 (3 小时)
- [ ] 端到端测试（完整 MCP 工作流）
- [ ] Mock 服务器测试（模拟 MCP 服务器）
- [ ] 性能测试（吞吐量和延迟）

#### 3. 错误处理和重试 (1 小时)
- [ ] 实现自动重试机制
- [ ] 超时处理
- [ ] 错误恢复

---

## 🎯 验收标准

- [ ] 所有工具/资源/提示词发现接口可用
- [ ] 10+ 集成测试通过
- [ ] 性能测试达标（延迟 < 100ms）
- [ ] 零已知 bug

---

## 📝 技术设计

### 工具发现机制

```rust
// src/mcp/tools.rs
impl ToolRegistry {
    /// 列出所有已注册的工具
    pub fn list_tools(&self) -> Vec<Tool> {
        self.tools.values().cloned().collect()
    }
}

// src/mcp/resources.rs
impl ResourceRegistry {
    /// 列出所有已注册的资源
    pub fn list_resources(&self) -> Vec<Resource> {
        self.resources.values().cloned().collect()
    }
}

// src/mcp/prompts.rs
impl PromptRegistry {
    /// 列出所有已注册的提示词模板
    pub fn list_prompts(&self) -> Vec<PromptTemplate> {
        self.prompts.values().cloned().collect()
    }
}
```

### 集成测试结构

```rust
// tests/mcp_integration_test.rs
#[tokio::test]
async fn test_mcp_end_to_end_workflow() {
    // 1. 创建 MCP 客户端
    // 2. 初始化握手
    // 3. 工具发现
    // 4. 调用工具
    // 5. 读取资源
    // 6. 获取提示词模板
    // 7. 关闭连接
}

#[tokio::test]
async fn test_mcp_mock_server() {
    // 使用 mockito 模拟 MCP 服务器
    // 测试各种场景
}
```

### 错误处理和重试

```rust
// src/mcp/client.rs
impl McpClient {
    /// 带重试的请求发送
    async fn send_request_with_retry(
        &self,
        request: JsonRpcRequest,
        max_retries: usize,
    ) -> Result<JsonRpcResponse, McpError> {
        let mut retries = 0;
        loop {
            match self.send_request(request.clone()).await {
                Ok(response) => return Ok(response),
                Err(e) if retries < max_retries => {
                    retries += 1;
                    tokio::time::sleep(Duration::from_millis(100 * retries as u64)).await;
                }
                Err(e) => return Err(e),
            }
        }
    }
}
```

---

**最后更新**: 2026-03-11 03:30 UTC+8
